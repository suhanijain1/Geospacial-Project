# Constants

# Constants
Attributes:
| Name | Type | Description |
| --- | --- | --- |
| `TAG_HIDDEN` |  | Tag to hide a node/edge from certain tracing/streaming environments. |
| `TAG_NOSTREAM` |  | Tag to disable streaming for a chat model. |
| `START` |  | The first (maybe virtual) node in graph-style Pregel. |
| `END` |  | The last (maybe virtual) node in graph-style Pregel. |
## TAG\_HIDDEN `module-attribute` [¶](#langgraph.constants.TAG_HIDDEN "Permanent link")
```
TAG_HIDDEN = intern('langsmith:hidden')
```
Tag to hide a node/edge from certain tracing/streaming environments.
## TAG\_NOSTREAM `module-attribute` [¶](#langgraph.constants.TAG_NOSTREAM "Permanent link")
```
TAG_NOSTREAM = intern('nostream')
```
Tag to disable streaming for a chat model.
## START `module-attribute` [¶](#langgraph.constants.START "Permanent link")
```
START = intern('__start__')
```
The first (maybe virtual) node in graph-style Pregel.
## END `module-attribute` [¶](#langgraph.constants.END "Permanent link")
```
END = intern('__end__')
```
The last (maybe virtual) node in graph-style Pregel.
Back to top

[Source](https://langchain-ai.github.io/langgraph/reference/constants/)
